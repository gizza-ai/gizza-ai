[package]
name = "gizza-ai-postman-collection-converter-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"

[dependencies]
# preserve_order: re-serialized JSON bodies keep the author's key order.
serde_json = { version = "1", features = ["preserve_order"] }
