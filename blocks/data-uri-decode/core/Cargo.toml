[package]
name = "gizza-ai-data-uri-decode-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"

[dependencies]
base64 = "0.22"
hex = "0.4"
serde = { version = "1", features = ["derive"] }
# reuse the magic-byte sniffer to label decoded binary payloads.
gizza-ai-detect-file-type-core = { path = "../../detect-file-type/core" }
