[package]
name = "gizza-ai-barcode-decode-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"

[dependencies]
image = { version = "0.25", default-features = false, features = ["png", "jpeg", "gif", "bmp", "webp"] }
rxing = { version = "0.7", default-features = false }
