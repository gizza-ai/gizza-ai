{
  "name": "gizza-ai/charcode-codec",
  "version": "0.1.0",
  "interface": "handler@v1",
  "summary": "Convert text to a list of Unicode, UTF-8, UTF-16 or ASCII character codes in any base, and back.",
  "role": "skill",
  "tool": {
    "description": "Convert text into a list of numeric character codes and rebuild text from such a list. Use mode='encode' (default, e.g. 'Hello' -> '72 101 108 108 111') or mode='decode' (e.g. '72 101 108 108 111' -> 'Hello'). base picks the number base: 'dec' (default), 'hex', 'bin' or 'oct'. scope picks what one code counts: 'unicode-scalar' (default, one code per code point, so 😀 is 128512), 'utf8-bytes' (240 159 152 128), 'utf16-units' (55357 56832) or 'ascii' (rejects characters above 127). delimiter sets the separator when encoding ('space' default, 'comma', 'newline', 'none'); prefix adds '0x', '\\x' or 'U+' before each code; padding='fixed' zero-pads each code to the scope's natural width; uppercase controls hex digit case. Decoding is tolerant: it ignores whitespace and common separators, strips 0x/\\x/\\u/U+/0b/0o prefixes, and splits an unseparated run of fixed-width codes, so any encoded form round-trips. Out-of-range codes, surrogates, invalid UTF-8 and digits illegal in the chosen base are reported as errors. format='json' returns the numeric codes as an array alongside the result.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "base": {
          "default": "dec",
          "description": "Number base the codes are written in: 'dec' decimal (default, e.g. 72), 'hex' hexadecimal (48), 'bin' binary (1001000) or 'oct' octal (110).",
          "enum": [
            "dec",
            "hex",
            "bin",
            "oct"
          ],
          "type": "string"
        },
        "delimiter": {
          "default": "space",
          "description": "Separator placed between codes when encoding: 'space' (default), 'comma' (', '), 'newline', or 'none' for one unbroken run. Decoding ignores all of these regardless.",
          "enum": [
            "space",
            "comma",
            "newline",
            "none"
          ],
          "type": "string"
        },
        "format": {
          "default": "text",
          "description": "Output shape: 'text' (default) returns just the codes (or the decoded text); 'json' returns an object with mode, base, scope, count, the numeric codes array, and the result.",
          "enum": [
            "text",
            "json"
          ],
          "type": "string"
        },
        "input": {
          "description": "The text to convert into character codes, or the list of codes to turn back into text (e.g. '72 101 108 108 111').",
          "type": "string"
        },
        "mode": {
          "default": "encode",
          "description": "Direction: 'encode' (default) turns text into character codes, 'decode' turns a list of codes back into text.",
          "enum": [
            "encode",
            "decode"
          ],
          "type": "string"
        },
        "padding": {
          "default": "none",
          "description": "Zero-padding when encoding: 'none' (default) writes the shortest form, 'fixed' pads every code to the scope's natural width (ASCII decimal 072, UTF-8 binary 01001000, Unicode hex a 4-digit minimum like 0041). Ignored on decode.",
          "enum": [
            "none",
            "fixed"
          ],
          "type": "string"
        },
        "prefix": {
          "default": "none",
          "description": "Marker placed before each code when encoding: 'none' (default), '0x' (0x48), '\\x' (\\x48) or 'U+' (U+0048). Decoding strips 0x, \\x, \\u, U+, 0b and 0o automatically.",
          "enum": [
            "none",
            "0x",
            "\\x",
            "U+"
          ],
          "type": "string"
        },
        "scope": {
          "default": "unicode-scalar",
          "description": "What one code counts: 'unicode-scalar' (default) one code per Unicode code point, so 😀 is 128512; 'utf8-bytes' one code per UTF-8 byte (240 159 152 128); 'utf16-units' one code per UTF-16 code unit (55357 56832); 'ascii' one code per character, rejecting anything above 127.",
          "enum": [
            "unicode-scalar",
            "utf8-bytes",
            "utf16-units",
            "ascii"
          ],
          "type": "string"
        },
        "uppercase": {
          "default": true,
          "description": "When true (default), hexadecimal codes use uppercase A-F, matching the U+1F600 convention; set false for lowercase (1f600). Decoding accepts either case.",
          "type": "boolean"
        }
      },
      "required": [
        "input"
      ],
      "type": "object"
    }
  }
}
