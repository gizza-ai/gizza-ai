[package]
name = "gizza-ai-age-calculator-web"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
gizza-ai-age-calculator-core = { path = "../core" }
chrono = { version = "0.4", default-features = false, features = ["alloc"] }
js-sys = "0.3"
