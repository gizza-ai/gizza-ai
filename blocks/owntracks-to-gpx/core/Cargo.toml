[package]
name = "gizza-ai-owntracks-to-gpx-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"

[dependencies]
# serde_json: pure-Rust, wasm-safe JSON parser (proven across many gizza blocks).
# Parses both the OwnTracks JSON export and each .rec line's JSON payload.
serde_json = "1"
