[package]
name = "gizza-ai-trim-audio-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"

[dependencies]
# Shared duration-free areverse fade chain (same trick, one implementation).
gizza-ai-audio-fade-core = { path = "../../audio-fade/core" }
