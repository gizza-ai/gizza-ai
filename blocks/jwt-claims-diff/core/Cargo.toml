[package]
name = "gizza-ai-jwt-claims-diff-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
chrono = { version = "0.4", default-features = false, features = ["alloc"] }
gizza-ai-jwt-decode-core = { path = "../../jwt-decode/core" }

[dev-dependencies]
base64 = "0.22"
