[package]
name = "gizza-ai-document-skew-detector-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"

[dependencies]
# Pure-Rust decode + resize + grayscale. default-features off; common web formats.
image = { version = "0.25", default-features = false, features = ["png", "jpeg", "webp", "gif", "bmp"] }
