[package]
name = "gizza-ai-age-keygen-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"

[dependencies]
age = { version = "0.12.1", default-features = false }
chrono = { version = "0.4", default-features = false, features = ["clock", "std", "wasmbind"] }
serde_json = "1"
