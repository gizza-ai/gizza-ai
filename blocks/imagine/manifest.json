{
  "name": "gizza-ai/imagine",
  "version": "0.1.0",
  "interface": "handler@v1",
  "summary": "Generate an image from a text prompt.",
  "role": "skill",
  "tool": {
    "description": "Generate an image from a text prompt using Janus-Pro-1B on WebGPU. Renders inline in the chat as PNG. Requires WebGPU with shader-f16.",
    "parameters": {
      "type": "object",
      "required": ["prompt"],
      "properties": {
        "prompt": { "type": "string", "description": "Description of the image to generate (e.g. 'a cat reading a book in a sunlit window')." }
      },
      "additionalProperties": false
    }
  }
}
