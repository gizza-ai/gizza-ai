{
  "name": "gizza-ai/diff-highlight",
  "version": "0.1.0",
  "interface": "handler@v1",
  "summary": "Render a diff as a syntax-highlighted PNG",
  "role": "skill",
  "tool": {
    "description": "Render a unified diff or two raw texts into a shareable PNG image with syntax highlighting, add/remove tinting, optional side-by-side or unified layout, line numbers, intra-line change highlighting, whitespace-ignore comparison and configurable context. Use this when the user wants a picture of a diff for PRs, chats or release notes rather than text output. Returns an image, not text.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "context": {
          "default": 3,
          "description": "Unchanged context lines kept around each change before long runs collapse into a note. Default 3, maximum 50.",
          "maximum": 50,
          "minimum": 0,
          "type": "integer"
        },
        "diff": {
          "default": "",
          "description": "Unified diff / patch text to render. Use this OR provide both `left` and `right`; leave blank when comparing two raw texts.",
          "type": "string"
        },
        "ignore_whitespace": {
          "default": false,
          "description": "Treat lines that differ only in whitespace as unchanged. Default false.",
          "type": "boolean"
        },
        "language": {
          "default": "",
          "description": "Optional syntax language hint such as `rust`, `python`, `javascript`, `typescript`, `bash`, `go`, `yaml` or `markdown`. Unknown or blank renders as plaintext.",
          "type": "string"
        },
        "layout": {
          "default": "side-by-side",
          "description": "Image layout: `side-by-side` (default, old left / new right) or `unified` (classic inline -/+ rows).",
          "enum": [
            "side-by-side",
            "unified"
          ],
          "type": "string"
        },
        "left": {
          "default": "",
          "description": "Original text for two-text comparison mode. Provide with `right`; leave blank when using `diff`.",
          "type": "string"
        },
        "line_numbers": {
          "default": true,
          "description": "Show old/new line number gutters. Default true.",
          "type": "boolean"
        },
        "right": {
          "default": "",
          "description": "Modified text for two-text comparison mode. Provide with `left`; leave blank when using `diff`.",
          "type": "string"
        },
        "theme": {
          "default": "",
          "description": "Optional syntect theme name such as `base16-ocean.dark`, `Solarized (dark)`, `Solarized (light)` or `InspiredGitHub`. Blank or unknown falls back to a dark theme.",
          "type": "string"
        },
        "word_highlight": {
          "default": true,
          "description": "Highlight the changed character span inside paired changed lines. Default true.",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  }
}
