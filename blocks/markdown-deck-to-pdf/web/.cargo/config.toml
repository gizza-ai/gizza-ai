# lopdf's dependency tree pulls getrandom 0.3, which requires an explicit
# backend cfg for wasm32-unknown-unknown. Scoped to this web crate's wasm-pack build.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
