[package]
name = "gizza-ai-rsa-encrypt-web"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
gizza-ai-rsa-encrypt-core = { path = "../core" }
# rsa pulls getrandom 0.2 transitively (OsRng); the page targets
# wasm32-unknown-unknown which needs the js backend.
getrandom = { version = "0.2", features = ["js"] }
