[package]
name = "gizza-ai-calendar-heatmap-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"

# Pure SVG string building + hand-rolled Gregorian date math — no dependencies.
[dependencies]
