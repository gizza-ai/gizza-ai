[package]
name = "gizza-ai-matrix-text-parser-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"

[dependencies]
# preserve_order keeps the report keys in the order they are inserted.
serde = "1"
serde_json = { version = "1", features = ["preserve_order"] }
