[package]
name = "gizza-ai-bcrypt-hash-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"

[dependencies]
# default features include the OsRng-backed salt generator; getrandom 0.2 works
# on wasm32-wasip1 (wafer clock/rng) and the page enables its js backend.
bcrypt = "0.15"
